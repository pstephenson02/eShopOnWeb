pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'dotnet'
            }
        }
        stage('Test') {
            steps {
                echo 'Testing'
            }
        }
    }
}
